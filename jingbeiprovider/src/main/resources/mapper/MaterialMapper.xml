<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jb.mapper.material.MaterialMapper">

    <!-- 查询总条数 -->
    <select id="queryMaterialPageCount" parameterType="map" resultType="int">
        select count(*) from bill_of_materials m
        left join t_channel c on m.channel_id = c.id
    </select>


    <!-- 分页查询 -->
    <select id="queryMaterialPageList" parameterType="map" resultType="com.jb.model.BillOfMaterials">
        select
        m.id as id,
        m.name as name,
        m.time as time,
        m.channel_id as channelId,
        c.`name` as channelName
        from bill_of_materials m
        left join t_channel c on m.channel_id = c.id
        LIMIT #{startIndex,jdbcType=NUMERIC},#{rows,jdbcType=NUMERIC}
    </select>



    <select id="queryMaterials"  resultType="com.jb.model.BillOfMaterials">
        select
        m.id as id,
        m.name as name,
        m.time as time,
        m.channel_id as channelId,
        c.`name` as channelName
        from bill_of_materials m
        left join t_channel c on m.channel_id = c.id
    </select>

</mapper>